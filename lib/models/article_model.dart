class ArticleModel {
  String author, title, description, url, urlToImage, content;

  ArticleModel({
    this.author,
    this.title,
    this.url,
    this.urlToImage,
    this.content,
    this.description,
  });
}
